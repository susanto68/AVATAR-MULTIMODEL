/**
 * Placeholder for Vercel server-only exposure; this repo is not Next.js,
 * but if deployed to a Next project, this config keeps OPENAGENTS_KEY server-side.
 */
const nextConfig = {
	reactStrictMode: true,
	webpack: (config) => config,
};

module.exports = nextConfig;


